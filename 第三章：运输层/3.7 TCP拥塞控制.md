# 3.7 TCP拥塞控制

## 3.7.1 经典的拥塞控制

端到端控制拥塞

TCP发送方怎样确定发送的速率: 

1. 一个丢失的报文意味着拥塞, 因此当丢失报文段时应降低TCP发送方的速率
2. 报文段确认到达时, 增加发送方的速率
3. 带宽检测

方法(书p176):

1. 慢启动(快速的增加至最大速率(指数))
2. 拥塞避免(谨慎的增加速率(线性))
3. 快速恢复

慢启动和拥塞避免时必须的, 两者是相互结合的使用